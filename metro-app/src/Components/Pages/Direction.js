import React from 'react';
import PropTypes from 'prop-types';

const Direction = () => {
    return (
        <div>
            
        </div>
    );
};

Direction.propTypes = {};

export default Direction;