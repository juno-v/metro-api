import React from 'react';
import PropTypes from 'prop-types';

const NotFound = () => {
    return (
        <div>
            <h1> Default to render, routes not match/founed. Try again later. </h1>
        </div>
    );
};

NotFound.propTypes = {};

export default NotFound;